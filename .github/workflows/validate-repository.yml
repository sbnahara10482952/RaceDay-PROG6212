name: Validate Repository

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate repository folders
        run: |
          test -d docs
          test -d .github/workflows
          echo "Required repository folders exist"

      - name: Validate required documentation
        run: |
          test -s docs/RaceDay_API_Endpoint_Plan.pdf
          test -s docs/RaceDay_Database.sql
          test -s docs/RaceDay_ERD.png
          test -s docs/RaceDay_ERD.drawio
          test -s docs/SQL_Screenshots.pdf
          echo "All required documentation files exist and are not empty"

      - name: Validate README
        run: |
          test -f README.md
          test -s README.md
          echo "README exists and contains content"

      - name: Validate gitignore
        run: |
          test -f .gitignore
          echo ".gitignore exists"
